---
import DocsLayout from "@/layouts/DocsLayout.astro";
import Showcase from "@/components/astro/Showcase.astro";
import AccordionDemo from "../../components/ui/Accordion.astro";

const accordionCode = `<div x-data="{ open: null }" class="w-full max-w-2xl mx-auto divide-y divide-gray-200 dark:divide-zinc-700 border border-gray-200 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900">
  <!-- Item 1 -->
  <div class="overflow-hidden">
    <div
      @click="open === 1 ? open = null : open = 1"
      class="flex justify-between items-center cursor-pointer px-4 py-3 text-gray-800 dark:text-white font-medium transition-colors"
    >
      <span>What is Alpine.js?</span>
      <svg
        :class="open === 1 ? 'rotate-180' : ''"
        class="w-5 h-5 text-gray-500 transition-transform duration-200"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        viewBox="0 0 24 24"
      >
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
      </svg>
    </div>

    <div
      x-show="open === 1"
      x-collapse
      class="px-4 pb-2 py-3 text-sm text-gray-600 dark:text-gray-300 leading-relaxed"
    >
      Alpine.js is a minimal JavaScript framework that lets you write declarative interactivity
      directly in your HTML.
    </div>
  </div>

  <!-- Item 2 -->
  <div class="overflow-hidden">
    <div
      @click="open === 2 ? open = null : open = 2"
      class="flex justify-between items-center cursor-pointer px-4 py-3 text-gray-800 dark:text-white font-medium transition-colors"
    >
      <span>Can I use it with Tailwind CSS?</span>
      <svg
        :class="open === 2 ? 'rotate-180' : ''"
        class="w-5 h-5 text-gray-500 transition-transform duration-200"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        viewBox="0 0 24 24"
      >
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
      </svg>
    </div>

    <div
      x-show="open === 2"
      x-collapse
      class="px-4 pb-2 py-3 text-sm text-gray-600 dark:text-gray-300 leading-relaxed"
    >
      Yes! Alpine.js and Tailwind CSS are often used together for lightweight, modern interfaces.
    </div>
  </div>

  <!-- Item 3 -->
  <div class="overflow-hidden">
    <div
      @click="open === 3 ? open = null : open = 3"
      class="flex justify-between items-center cursor-pointer px-4 py-3 text-gray-800 dark:text-white font-medium transition-colors"
    >
      <span>Is Alpine.js reactive?</span>
      <svg
        :class="open === 3 ? 'rotate-180' : ''"
        class="w-5 h-5 text-gray-500 transition-transform duration-200"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        viewBox="0 0 24 24"
      >
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
      </svg>
    </div>

    <div
      x-show="open === 3"
      x-collapse
      class="px-4 pb-2 py-3 text-sm text-gray-600 dark:text-gray-300 leading-relaxed"
    >
      Absolutely! Alpine.js provides reactivity similar to Vue or React, but is much lighter.
    </div>
  </div>
</div>
`;
---

<DocsLayout title="Accordion">
  <header class="flex flex-wrap justify-between gap-4 mt-20">
    <div class="prose dark:prose-invert">
      <h1 id="accordion-component">Accordion Components</h1>
      <p>
        A component created to complement the FAQ section in the Landing page,
        created with Tailwind CSS V4 and Alphine JS as an interactive library
        for javascript.
      </p>
    </div>
  </header>

  <Showcase code={accordionCode}>
    <AccordionDemo />
  </Showcase>
  <section class="max-w-7xl mx-auto my-10 font-sans">
    <!-- Judul Utama -->
    <h2
      class="text-2xl font-extrabold bg-blue-400 border-4 border-black px-6 py-3 shadow-[6px_6px_0_0_#000] dark:shadow-[8px_8px_0_0_#fff] uppercase tracking-wide"
    >
      How To Use ?
    </h2>

    <!-- Container Utama -->
    <div
      class="mt-4 border-4 border-black p-6 shadow-[8px_8px_0_0_#000] dark:shadow-[8px_8px_0_0_#fff] bg-white space-y-6"
    >
      <!-- Step 1 -->
      <div
        class="border-4 border-black rounded-xl bg-indigo-200 shadow-[4px_4px_0_0_#000] p-4"
      >
        <div class="flex items-center gap-3 mb-3">
          <span
            class="bg-gray-900 text-white font-bold px-2 py-1 rounded shadow-[2px_2px_0_0_#000]"
            >1</span
          >
          <h3 class="text-lg font-bold uppercase text-gray-900">
            Install the following dependencies:
          </h3>
        </div>

        <div
          class="bg-black text-white p-4 font-mono text-sm rounded shadow-[4px_4px_0_0_#000]"
        >
          npm install tailwindcss
        </div>

        <div
          class="bg-black text-white p-4 mt-3 font-mono text-sm rounded shadow-[4px_4px_0_0_#000]"
        >
          &lt;script defer src="https://cdn.jsdelivr.net/ npm/alpinejs@3.x.x/
          dist/cdn.min.js" is:inline /&gt;
        </div>
      </div>

      <!-- Step 2 -->
      <div
        class="border-4 border-black rounded-xl bg-indigo-300 shadow-[4px_4px_0_0_#000] p-4"
      >
        <div class="flex items-center gap-3 mb-3">
          <span
            class="bg-gray-900 text-white font-bold px-2 py-1 rounded shadow-[2px_2px_0_0_#000]"
            >2</span
          >
          <h3 class="text-lg font-bold uppercase text-gray-900">
            Copy and paste the Display Code Into Your Project.
          </h3>
        </div>
      </div>
  </section>
</DocsLayout>
